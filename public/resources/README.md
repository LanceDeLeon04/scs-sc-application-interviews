# Resources

Drop project assets here (e.g. `SCSLogo.png`).

To actually show the logo in the app, copy it into `public/` (e.g.
`public/SCSLogo.png`) and reference it as `/SCSLogo.png` from an `<img>` tag
— files in `resources/` are not automatically bundled or served by Vite.
